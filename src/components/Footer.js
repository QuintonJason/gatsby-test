import React from 'react';
import Link from 'gatsby-link';

const BlogLink = (props) => {
  return (
    <footer className="post-landing-link">
      <h1>This is the footer</h1>
    </footer>
  );
};

export default BlogLink;